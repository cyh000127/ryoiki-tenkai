import { GestureConsole } from "../../widgets/gesture-console/GestureConsole";
import { AppShell } from "../../platform/ui/AppShell";

export function GestureControlPage() {
  return (
    <AppShell>
      <GestureConsole />
    </AppShell>
  );
}
