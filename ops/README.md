# Operations Skeleton

This directory is reserved for versioned operational configuration, runbooks, and launch checks.

Do not hardcode operational policy values directly in frontend pages or backend route handlers.
